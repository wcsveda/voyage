extends ./boilerplate/skeleton.pug

block head
  title VOYAGE: Your Traval Planner 
  
block body
  #container 
    mixin icon(name)
      span(class=['fa', 'fa-' + name])

    header
      include ./parts/01-contact-nav.pug
      include ./parts/02-brand-nav.pug